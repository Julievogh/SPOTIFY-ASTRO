---
export interface Props {
  icon: string;
}

const { icon } = Astro.props as Props;
const { default: innerHTML } = await import(`/src/icons/${icon}.svg?raw`);
---

<Fragment set:html={innerHTML} class="small-icon" />

<style>
  .small-icon {
    width: 20px;
    height: 20px;
  }
</style>
